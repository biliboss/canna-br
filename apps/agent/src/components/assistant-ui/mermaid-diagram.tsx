"use client";

/**
 * mermaid-diagram — vendored stub.
 *
 * Depends on `beautiful-mermaid` which is a workspace-internal package in the
 * assistant-ui monorepo. Stubbed for the canna-br agent app. To enable Mermaid
 * rendering, vendor beautiful-mermaid or use a published alternative.
 */
import type { SyntaxHighlighterProps } from "@assistant-ui/react-markdown";

export function MermaidDiagram(_props: SyntaxHighlighterProps) {
  return null;
}
