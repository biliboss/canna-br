"use client";

/**
 * heat-graph — vendored stub.
 *
 * Depends on `heat-graph` which is a workspace-internal package in the
 * assistant-ui monorepo. Stubbed for the canna-br agent app which does not
 * render activity heat graphs.
 */

export function HeatGraph(_props: { data: unknown[] }) {
  return null;
}
